<h1>All Ads</h1>
<% @ads.each do |post| %>
   <p><%= link_to ad.title, ad %></p>
   <div class="media">
     <div class="media-body">
       <h4 class="media-heading">
         <%= link_to ad.title, ad %>
       </h4>
     </div>
   </div>
<% end %>